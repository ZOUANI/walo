pipeline {
    agent any
    stages {
        stage('colne') {
            steps {
                sh 'git pull https://github.com/ZOUANI/walo.git'
            }
        }
        stage('build') {
                    steps {
                        sh 'cd myprojects/walo'
                        sh 'mvn package'
                    }
                }

        stage('run') {
                    steps {
                      sh 'cd myprojects/walo'
                      sh 'java -jar walo*.jar'
                    }
                }
            }
    
}
